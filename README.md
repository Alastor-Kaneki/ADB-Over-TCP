# PixelForge temporary CI branch

Build-only branch for compiling the clean-room PixelForge Android editor. The Pixilart APK is used only as a feature and UI-reference map.

Current build: v0.4.0 — HEX colors + Pixilart-style tool/layer/frame/settings drawers.
