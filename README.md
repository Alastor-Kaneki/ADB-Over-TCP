# PixelForge temporary CI branch

Build-only branch for compiling the clean-room PixelForge Android editor. The Pixilart APK is used only as a feature reference.

Current build: v0.3.0 Forge UI + power tools, CI trigger.
